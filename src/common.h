#pragma once

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

// NOTE: Generics don't exist, so I'll think about this one
// typedef struct {
//     int count;
//     int capacity;
//
// } List;
